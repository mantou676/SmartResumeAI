#!/bin/bash
set -e

# 阿里云 FC Custom Runtime 启动脚本
# FC 会将请求转发到本脚本启动的 HTTP 服务 (端口 9000)

# 安装依赖 (首次冷启动)
pip install -r /code/requirements.txt -i https://mirrors.aliyun.com/pypi/simple/ -q 2>/dev/null || true

# 启动 FastAPI
exec python3 -m uvicorn main:app --host 0.0.0.0 --port 9000 --log-level info
